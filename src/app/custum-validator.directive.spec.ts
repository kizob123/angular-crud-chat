import { CustumValidatorDirective } from './custum-validator.directive';

describe('CustumValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new CustumValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
